apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "k3sdemo.fullname" . }}-test-connection"
  labels:
    {{- include "k3sdemo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "k3sdemo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
