apiVersion: v1
kind: Service
metadata:
  name: {{ include "k3sdemo.fullname" . }}
  labels:
    {{- include "k3sdemo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k3sdemo.selectorLabels" . | nindent 4 }}
